services:
  db:
    image: postgres:16
    environment:
      POSTGRES_DB: tanfolyam
      POSTGRES_USER: django
      POSTGRES_PASSWORD: django
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
